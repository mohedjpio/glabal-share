services:
  - type: web
    name: smartshare
    runtime: node
    buildCommand: npm install --production
    startCommand: node server/index.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
